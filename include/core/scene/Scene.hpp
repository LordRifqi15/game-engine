#pragma once
// Task 040: Scene container & lifecycle (new location per spec)
// This header provides Engine::Scene alias and includes the existing engine::Scene.
#include "core/scene.h"

namespace Engine {
    using Scene = ::engine::Scene;
} // namespace Engine
